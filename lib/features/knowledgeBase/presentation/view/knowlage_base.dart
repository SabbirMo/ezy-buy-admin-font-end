import 'package:flutter/material.dart';

class KnowlageBase extends StatelessWidget {
  const KnowlageBase({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Knowlage Base"),
      ),
    );
  }
}
