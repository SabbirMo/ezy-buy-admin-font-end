import 'package:flutter/material.dart';

class CetagoriesScreen extends StatelessWidget {
  const CetagoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Cetagories"),
      ),
    );
  }
}
